{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install --prefix server && npm run seed --prefix server"
  },
  "deploy": {
    "startCommand": "npm run start --prefix server",
    "restartPolicyType": "ON_FAILURE"
  }
}
